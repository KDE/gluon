import QtQuick 1.1

Rectangle {
	y: (parent.y - 10)
	height: (parent.height + 20)
	x: (parent.x - 10)
	width: (parent.width + 20)
	border.color: "lightgray";
	color: "white";
	radius: 10;
	opacity: 0.65;
}
