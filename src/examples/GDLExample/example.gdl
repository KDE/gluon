{ GameObject(root)
    { Asset(PaddleSprite)
        filename string(paddle.png)
    }
    
    { GameObject(Paddle)
        { KeyboardInputComponent(PaddleController)
            
        }
        { CollisionComponent(PaddleCollision)
        }
        { SpriteRendererComponent(PaddleRenderer)
            sprite asset(PaddleSprite)
        }
    }

    { Asset(BlockSprite)
        filename string(block.png)
    }
    
    { GameObject(Block)
        { CollisionComponent(BlockCollision)
        }
        { SpriteRendererComponent(BlockRenderer)
            sprite asset(BlockSprite)
        }
    }


    { Asset(BallSprite)
        filename string(ball.png)
    }
    
    { GameObject(Ball)
        { SpriteRendererComponent(BallRenderer)
            sprite asset(BallSprite)
        }
    }

}