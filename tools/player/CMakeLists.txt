add_subdirectory(qt)

if(WITH_KDE)
    find_package(KDE4)
    if(KDE4_FOUND)
        include_directories(${KDE4_INCLUDES})
        add_subdirectory(kde)
    else(KDE4_FOUND)
        message(STATUS "WITH_KDE is enabled but KDE libraries are not found - not building the KDE player tool")
    endif(KDE4_FOUND)
endif(WITH_KDE)
